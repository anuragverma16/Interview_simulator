# Local dev — Vite proxies /api to the backend (see vite.config.ts)
VITE_API_URL=/api/v1

# Production (Vercel) — set in Vercel Dashboard → Settings → Environment Variables
# VITE_API_URL=https://savoriabackend.sengarinfotech.com/api/v1
