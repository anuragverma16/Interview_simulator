NODE_ENV=production
PORT=5000

# MongoDB Atlas (required in production)
MONGODB_URI=

# Auth
JWT_SECRET=
JWT_REFRESH_SECRET=

# CORS — your live frontend URL
CLIENT_URL=https://interview-simulator-beta-henna.vercel.app
# Extra origins (comma-separated), optional
# CORS_ORIGINS=

# AI
GEMINI_API_KEY=

# Email (Gmail SMTP example)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=InterviewIQ AI <noreply@interviewiq.ai>
